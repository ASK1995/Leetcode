import math

class Solution:
    def numberOfMatches(self, n: int) -> int:
        matches = 0
        
        while(n != 1):
            matches += math.floor(n//2)
            n = math.ceil(n/2)
            print(matches, n)
        
        return matches
    