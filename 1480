class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        sum_till_now = 0
        res = []
        
        for num in nums:
            sum_till_now += num
            res.append(sum_till_now)
        
        return res