class Solution:
    def shuffle(self, nums: List[int], n: int) -> List[int]:
        ans = []
        start_index = 0

        while(start_index < n):
            ans.append(nums[start_index])
            ans.append(nums[start_index + n])
            start_index += 1
        
        return ans