class Solution:
    def xorOperation(self, n: int, start: int) -> int:
        i = 0
        total = 0

        while(i < n):
            total ^= (start + 2*i)
            i += 1
        
        return total