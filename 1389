class Solution:
    def createTargetArray(self, nums: List[int], index: List[int]) -> List[int]:
        res = []
        
        for value1, value2 in zip(nums, index):
            res.insert(value2, value1)
        
        return res