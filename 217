class Solution:
    def containsDuplicate(self, nums: List[int]) -> bool:
        num = set()
        
        for number in nums:
            if(number not in num):
                num.add(number)
            else:
                return True
        
        return False